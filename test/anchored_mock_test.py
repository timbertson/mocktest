import os
import sys

import helper
from mocktest import TestCase
from mocktest import mock_on, raw_mock, mock_wrapper
import mocktest

class AnchoredMockTest(TestCase):
	##TODO: # mock_on(parent) vs mock() vs mock_of(silent_mock)
			# mock_on(os).system should raise the second time
	pass